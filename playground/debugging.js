var person = {
    name: 'Andrew'
};

person.age = 25;

debugger;

person.name = 'Mike';

console.log(person);

// To debug in node.js use node inspect filename.js
// use n then enter to go to next line
// use repl to use node commands within debugger

